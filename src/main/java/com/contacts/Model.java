package com.contacts;

/** Base model type for domain data objects. */
public abstract class Model {

    /** Create a new model instance. */
    protected Model() {
        // Base model constructor
    }
}
